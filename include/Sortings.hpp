/// all sorting algrorithms: including merge sort...

class Sortings {
public:
  // merge sort
  template <typename Dtype>
  static void MergeSort(Dtype* input_array, const int N) {
    return;
  };

  // insertion sort
  template <typename Dtype>
  static void InsertionSort(Dtype* input_array, const int N) {
    return;
  }

  // quick sort
  template <typename Dtype>
  static void QuickSort(Dtype* input_array, const int N) {
    return;
  }
};
